**モデル:** Claude Sonnet 4.6

# 作業サマリー: vimmo-ls pygls 2.x 互換修正

**日付**: 2026-03-08

---

## 完了したタスク

1. `ls.workspace.get_document()` → `text_documents.get()` 移行（3箇所）
2. `show_message` / `publish_diagnostics` の pygls 2.x 互換ラッパー実装
3. LSP 定義ジャンプ修正（1-base/0-base オフセット、ClassDecl/FnDecl walk 漏れ）
4. 診断通知を `ls.protocol.notify()` 経由に修正
5. デバッグログを `/app/vimmo-ls.log`（ホストマウント）に出力するよう整備

## 変更ファイル

| ファイル | 変更内容 |
|---|---|
| `packages/vimmo-ls/src/vimmo_ls/server.py` | pygls 2.x 互換 API 対応、デバッグログ整備 |
| `packages/vimmo-ls/src/vimmo_ls/symbols.py` | 行番号 0-ベース変換、ClassDecl/FnDecl walk 追加 |
| `packages/vimmo-ls/tests/test_build_symbol_table.py` | 期待値を 0-ベースに修正 |
| `.gitignore` | `vimmo-ls.log` 追加 |

## テスト結果

`pytest packages/vimmo-ls/tests/` → **39 passed**

## 次のステップ

- 特になし。LSP の主要機能（補完・定義ジャンプ・診断）がすべて動作している
